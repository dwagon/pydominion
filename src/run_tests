#!/bin/sh

export PYTHONPATH=.
TEST=' -m unittest discover -p'

which -s coverage > /dev/null 2>&1
if [ "$?" == "0" ]; then
    coverage run ${TEST} "*.py"
    if [ "$?" == "0" ]; then
        coverage report -m | grep -v 100%
    else
        exit $?
    fi
else
    python ${TEST}
fi

#EOF
